import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#001747',
    },
    secondary: {
      main: '#063670',
    },
    tertiary: {
      main: '#E6EDf5',
    }
  },
});

export default theme;