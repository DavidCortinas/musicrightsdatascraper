import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0e1e2b',
    },
    secondary: {
      main: '#002048',
    },
    tertiary: {
      main: '#eff4f8',
    }
  },
});

export default theme;